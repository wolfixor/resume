# Secrets configuration for complex app
# Create shared-secret with these additional keys:
# kubectl create secret generic shared-secret \
#   --from-literal=MONGODB_URI=your-mongodb-uri \
#   --from-literal=JWT_SECRET=your-jwt-secret \
#   --from-literal=AI_DEEPSEEK_KEY=your-deepseek-key