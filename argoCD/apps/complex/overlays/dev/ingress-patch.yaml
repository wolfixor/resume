apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: complex-ingress
spec:
  tls:
  - hosts:
    - complexdev.wolfixsite.shop
    secretName: complex-dev-tls
  rules:
  - host: complexdev.wolfixsite.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: complex-service
            port:
              number: 80