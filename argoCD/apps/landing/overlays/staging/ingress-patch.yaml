apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: landingcomplex-ingress
spec:
  tls:
  - hosts:
    - staging.wolfixsite.shop
    secretName: landing-staging-tls
  rules:
  - host: staging.wolfixsite.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: landingcomplex-service
            port:
              number: 80