apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  labels:
    app: dashboard-app
spec:
  type: ClusterIP
  selector:
    app: dashboard-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP