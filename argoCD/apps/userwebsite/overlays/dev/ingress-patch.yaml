apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: userwebsite-ingress
spec:
  tls:
  - hosts:
    - userwebsitedev.wolfixsite.shop
    secretName: userwebsite-dev-tls
  rules:
  - host: userwebsitedev.wolfixsite.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: userwebsite-service
            port:
              number: 80