apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: userwebsite-ingress
spec:
  tls:
  - hosts:
    - userwebsite-staging.wolfixsite.shop
    secretName: userwebsite-staging-tls
  rules:
  - host: userwebsite-staging.wolfixsite.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: userwebsite-service
            port:
              number: 80